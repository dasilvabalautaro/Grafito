# Core — versiones fijas para compatibilidad con torch 2.2.2 en macOS x86_64
torch==2.2.2
torchvision==0.17.2
diffusers==0.27.2
transformers==4.39.3
accelerate==0.28.0
datasets==2.18.0
peft==0.10.0
huggingface_hub==0.23.5
safetensors>=0.4.0

# Image / utilities
Pillow>=10.0.0
numpy>=1.24.0,<2
tqdm>=4.65.0
click>=8.1.0
pyyaml>=6.0

# Demo
gradio==4.44.1
fastapi==0.114.2  # trae starlette<0.39; gradio 4.44 no soporta starlette moderno
pydantic==2.8.2   # gradio-client 1.3 no entiende los schemas de pydantic>=2.10

# Tracking (optional)
# wandb>=0.16.0

# Evaluation / v3 data filtering (optional)
lpips>=0.1.4
scikit-image>=0.22.0
opencv-python<4.10  # filtro facial en prepare_instruct_celeba y prepare_v3_mix

# Dev (optional)
# pytest>=8.0.0
# black>=24.0.0
# flake8>=7.0.0
# ipykernel>=6.29.0
# jupyter>=1.0.0
